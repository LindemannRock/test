{% extends '_layouts/base' %}

{% block content %}
	<div class="mx-auto max-w-4xl px-4 py-24 text-center">
		<h1 class="mb-6 text-4xl font-bold text-gray-900">{{ siteName }}</h1>
		<p class="text-lg text-gray-600">{{ 'Welcome to your new site.'|t(primaryTranslationCategory) }}</p>
	</div>
{% endblock %}
